odb:
    extends:
        file: odb/docker-compose.yml
        service: odb
api:
    extends:
        file: api/docker-compose.yml
        service: api
scheduler:
    extends:
        file: scheduler/docker-compose.yml
        service: scheduler
